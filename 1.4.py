# Array & Strings

# 1.4

"""
hash table to count the appearances of a letter
if even count all should be two
if odd count then one can be one
O(n + n/2) = O(n)
n to go through the string.
n/2 to go through the created table after checking for != 2
"""
